<!DOCTYPE html>
<html>
    <head>
        <title>Edit Note</title>
        <link rel="stylesheet" href="/styles.css" />
    </head>
    <body>
        <div class="container">
        <!-- <div class=""> -->
            <h2 class="margin">Edit Catatan</h2>
            <form
            action="/new-note/edit/<%= note.id %>"
            method="POST"
            class="form-wrapper"
            >
                <div class="form-group">
                    <label>Title</label>
                    <input type="text" name="title" value="<%= note.title %>" required />
                </div>
                <div class="form-group">
                    <label>Description</label>
                    <textarea name="description" required><%= note.description %></textarea><br /><br />
            </di>
            <button type="submit" class="btn-submit margin">Update</button>
            </form>
        <!-- </div> -->
        </div>
    </body>
</html>
